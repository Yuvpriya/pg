#include <stdio.h>
int main()
{
    int p,q;
    long long int r=1;
    scanf("%d",&p);
    scanf("%d",&q);
    while(q!= 0)
    {
        r*=p;
        q--;
    }

    printf("%lld",c);

    return 0;
}
